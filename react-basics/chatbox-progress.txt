LESSON 3

1. separate chat message from app component
    - create a component where you will store the message array
    - use map method to transform each array object to chat message components
    - return the chat messages
    - load it to app component

    ps. add key to each messages

2. make the chat input interactive
    - use event handler to capture text in text field
    - save to state variable 
    - use event handler in send button
    - run state setter function to rewrite the css of chat messages